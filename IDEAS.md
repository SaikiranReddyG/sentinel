# Parking Lot

Items deferred from the current v0.1 line:

- Rename `src/` to `sentinel/` and update packaging/imports accordingly.
- Add batched HTTP output for the event sink.
- Add missing IPv6 packet dispatch and protocol parsing.
- Expand the dashboard with richer summaries and filter controls.
- Add PCAP replay mode for offline regression checks.
- Add more rules and detector coverage for common service probes.
- Consider pluggable sink configuration for long-running deployments.