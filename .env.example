# Sentinel local overrides (copy to .env and edit)

# Path to codex-platform directory containing codex_bus.py.
# Relative paths are resolved from the sentinel project root.
CODEX_PLATFORM_PATH=../codex-platform

# Optional runtime defaults
# SENTINEL_CONFIG=config.yaml
# SENTINEL_INTERFACE=eth0
